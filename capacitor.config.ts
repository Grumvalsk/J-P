import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.grumvalski.julesandpitta',
  appName: 'JulesAndPitta',
  webDir: 'www'
};

export default config;
